# 参数与模型总览

> 文件：`318成功斜线版.ipynb`
> 更新：2026-03-21

---

## 一、物理常数与信号参数（Cell 4）

| 参数 | 变量名 | 值 | 单位 | 说明 |
|------|--------|----|------|------|
| 采样率 | `FS` | 250,000 | Hz | A/D 采样频率 |
| 声速 | `C` | 1500.0 | m/s | 水中声速（假设均匀） |
| 低截频 | `F_LOW` | 10,000 | Hz | LFM 起始频率 |
| 高截频 | `F_HIGH` | 30,000 | Hz | LFM 终止频率 |
| 中心频率 | `FC` | 20,000 | Hz | (F_LOW+F_HIGH)/2 |
| 带宽 | `BW` | 20,000 | Hz | F_HIGH - F_LOW |
| 脉冲宽度 | `T_PULSE` | 0.005 | s | 5 ms |
| 时带积 | `BT` | 100 | – | BW × T_PULSE |
| 阵元间距 | `D_ELEM` | 0.0375 | m | λ/2 @ 20 kHz |
| 阵元数 | `N_CH` | 16 | – | 线阵通道数 |
| 每脉冲采样点 | `N_SAMP` | 65,500 | 点 | |
| 总脉冲数 | `N_PULSE` | 96 | 帧 | 数据集帧数 |
| FFT 点数 | `N_FFT` | 65,536 | 点 | 2^⌈log₂(N_SAMP+N_REF)⌉ |
| 参考信号长度 | `N_REF` | 1,250 | 点 | T_PULSE × FS |
| 最大探测距离 | `MAX_RANGE_M` | 100.0 | m | |
| 最大距离采样数 | `MAX_R_SAMP` | ~33,333 | 点 | 2×MAX_RANGE_M/C×FS |
| 距离抽取因子 | `R_DECIM` | ~22 | – | MAX_R_SAMP // 1500 |
| 距离门数 | `N_RANGE` | ~1,515 | 门 | len(R_IDX) |

**距离轴：**
```
R_IDX  = np.arange(0, min(MAX_R_SAMP, N_FFT), R_DECIM)
R_AXIS = R_IDX × C / (2 × FS)   → 0 ~ 100 m，共 ~1515 点
```

---

## 二、波束成形（Cell 4 / 6 / 7）

### 2.1 波束配置

| 参数 | 值 | 说明 |
|------|----|------|
| `N_BEAMS_HL` | 90 | 高分辨率波束数（主用） |
| `BEAM_ANGLES` | −85° ~ +85°，均匀 90 点 | `np.linspace(-85, 85, 90)` |

### 2.2 导向矢量（假设：远场平面波、均匀线阵）

$$
s_{n}(\theta) = e^{j \cdot \phi_0 \cdot n \cdot \sin\theta}, \quad n = 0,1,\dots,15
$$

$$
\phi_0 = -\frac{2\pi f_c d}{c} \approx -0.5027 \text{ rad/element}
$$

```python
S_hl = make_steering(BEAM_ANGLES)   # shape: (16, 90), dtype: complex64
```

### 2.3 脉冲压缩（匹配滤波）

参考信号：LFM（线性调频）+ Hamming 窗

$$
\text{ref}(t) = \text{chirp}(f_0\!=\!10\text{kHz},\ f_1\!=\!30\text{kHz},\ T\!=\!5\text{ms}) \times w_{\text{Hamming}}
$$

匹配滤波器（频域共轭）：
$$
H_{mf}(f) = \overline{R(f)}
$$

### 2.4 GPU 处理流程（gpu_compress_bf）

```
输入 pulse_data: (16, 65500) float32
 ↓ FFT → 匹配滤波 → IFFT
 ↓ 提取距离门 R_IDX: (16, 1515)
 ↓ 常规波束形成 BF = S^H × snap: (90, 1515)
 ↓ 功率谱 |BF|²
输出: (1515, 90) float32
```

---

## 三、TPSW 归一化（Cell 8）

**两遍滑动窗白化（Two-Pass Sliding Window）**

$$
\tilde{X}[r,b] = 10\log_{10}(X[r,b]) - \overline{X}_r[r] - \overline{X}_b[b]
$$

| 步骤 | 操作 | 滑窗大小 | 轴 |
|------|------|----------|----|
| 1 | 转 dB | – | – |
| 2 | 距离维高通 | 80 点均值 | axis=0（距离）|
| 3 | 方位维高通 | 5 点均值 | axis=1（方位）|

```python
f  = 10 * np.log10(hl_raw[p] + 1e-30)
f -= uniform_filter1d(f, size=80, axis=0, mode='reflect')
f -= uniform_filter1d(f, size=5,  axis=1, mode='reflect')
```

**目的：** 去除距离/方位方向的背景混响，相当于 2D 空间高通滤波。

**近场屏蔽：**
```python
mask   = R_AXIS >= MASK_RANGE   # MASK_RANGE = 8.0 m
hl_use = hl_norm[:, mask, :]    # 仅保留 r ≥ 8 m
r_use  = R_AXIS[mask]           # 约 8 ~ 100 m
```

---

## 四、Kalman 滤波追踪（Cell 8）

### 4.1 可调参数

| 参数 | 当前值 | 建议值* | 说明 |
|------|--------|---------|------|
| `MASK_RANGE` | 8.0 m | 8.0 m | 近场屏蔽阈值（已在预处理阶段应用） |
| `SEARCH_RADIUS` | 2.0 m | **8.0 m** | 预测位置附近搜索半径 |
| `INNOV_GATE` | 2.0 m | **6.0 m** | 新息距离门限 |
| `MAX_COAST` | 1 帧 | **4 帧** | 最大连续 coasting 帧数 |
| `Q_STD` | 0.3 m | **1.0 m** | 过程噪声（目标加速度不确定性）|
| `R_STD` | 1.0 m | **1.5 m** | 观测噪声（检测位置误差）|
| `dt` | 1.0 | 1.0 | 归一化时间步（实际 PRI 未知）|

*建议值 = 尚未修改，下一步计划调整。

### 4.2 状态向量与运动模型

**状态：** $\mathbf{x} = [x,\ y,\ v_x,\ v_y]^\top$（笛卡尔坐标 + 速度）

**坐标约定：**
$$
x = r \sin\theta, \quad y = r \cos\theta \quad (\theta \text{ 为方位角})
$$

**匀速模型（状态转移矩阵 F）：**
$$
\mathbf{F} = \begin{bmatrix}1 & 0 & dt & 0\\0 & 1 & 0 & dt\\0 & 0 & 1 & 0\\0 & 0 & 0 & 1\end{bmatrix}, \quad dt = 1
$$

### 4.3 噪声矩阵

**过程噪声耦合矩阵 G（离散匀加速模型）：**
$$
\mathbf{G} = \begin{bmatrix}0.5\,dt^2 & 0 \\ 0 & 0.5\,dt^2 \\ dt & 0 \\ 0 & dt\end{bmatrix}
$$

$$
\mathbf{Q} = \sigma_Q^2 \cdot \mathbf{G}\mathbf{G}^\top \quad (\sigma_Q = Q\_STD = 0.3)
$$

**观测矩阵 H（仅观测位置）：**
$$
\mathbf{H} = \begin{bmatrix}1 & 0 & 0 & 0\\0 & 1 & 0 & 0\end{bmatrix}
$$

**观测噪声 R（各向同性）：**
$$
\mathbf{R} = \sigma_R^2 \cdot \mathbf{I}_2 \quad (\sigma_R = R\_STD = 1.0)
$$

### 4.4 滤波流程（每帧 p）

```
1. 预测
   x̂⁻ = F · x̂_{p-1}
   P⁻  = F · P_{p-1} · F^T + Q

2. 在预测位置 (pred_x, pred_y) 周围 SEARCH_RADIUS 内搜索最亮点
   → 若找到：meas_x, meas_y

3. 新息距离检验
   innov_dist = ‖z - H·x̂⁻‖
   ├─ ≤ INNOV_GATE: 标准 Kalman 更新
   │   K = P⁻·H^T·(H·P⁻·H^T + R)^{-1}
   │   x̂ = x̂⁻ + K·(z - H·x̂⁻)
   │   P = (I - K·H)·P⁻
   └─ > INNOV_GATE: coasting（仅预测）
       coast_count += 1
       └─ coast_count > MAX_COAST: 全局重搜，重置 P=5I₄
          （当前：速度也重置为 0，待修复）

4. 输出转回极坐标
   bright_r = √(kf_x² + kf_y²)
   bright_b = arctan2(kf_x, kf_y)  [deg]
```

### 4.5 初始化

- 第 0 帧取 `hl_use[0]` 全局 argmax 作为初始位置
- 初始速度：$v_x = v_y = 0$
- 初始协方差：$P_0 = 5 \cdot \mathbf{I}_4$

---

## 五、数据预处理与坐标映射（Cell 20）

### 5.1 输入归一化

每帧独立 min-max 归一化到 [0, 1]：

$$
\tilde{X}_p = \frac{X_p - \min(X_p)}{\max(X_p) - \min(X_p) + \epsilon}, \quad \epsilon = 10^{-8}
$$

输入形状：`(96, ~1395, 90)`  → 输出：`(96, ~1395, 90)` ∈ [0, 1]

### 5.2 Resize（双线性插值）

$$
(1395, 90) \xrightarrow{\text{bilinear}} (256, 64)
$$

- 距离维缩放比：1395 / 256 ≈ **5.45×**
- 方位维缩放比：90 / 64 ≈ **1.41×**
- `align_corners=False`

### 5.3 物理坐标轴（resize 后）

```
r_axis_rs = linspace(r_use[0],      r_use[-1],       IMG_H=256)  → [~8 m, ~100 m]
b_axis_rs = linspace(BEAM_ANGLES[0], BEAM_ANGLES[-1], IMG_W=64)  → [−85°, +85°]
```

---

## 六、伪标签生成（Cell 20）

**方法：** 在 Kalman 输出位置 `(bright_r[p], bright_b[p])` 处生成 Gaussian blob

$$
\text{label}[r,b] = \delta_{(r_i, b_i)} \xrightarrow{\text{Gaussian filter}(\sigma)} \frac{\cdot}{\max(\cdot)}
$$

| 参数 | 值 | 说明 |
|------|-----|------|
| `LABEL_SIGMA` | 3.0 px | 高斯标准差（resize 后像素空间） |
| 归一化 | 峰值 = 1.0 | `label /= label.max()` |

**当前问题：** 对所有 96 帧生成标签，包含 Kalman 失锁帧（坏标签）。
修好 Kalman 后此问题自动消除。

---

## 七、UNet 模型架构（Cell 18 / 22）

### 7.1 超参数

| 参数 | 变量名 | 值 | 说明 |
|------|--------|----|------|
| 输入高度 | `IMG_H` | 256 | 距离维 |
| 输入宽度 | `IMG_W` | 64 | 方位维 |
| 输入通道 | `N_CONTEXT` | 1 | =1：仅当前帧；=3：前+当+后 |
| 基础通道数 | `BASE_CH` | 16 | 编码器第1层通道数 |
| 深度 | `DEPTH` | 4 | 下采样层数 |

### 7.2 层结构（BASE_CH=16，DEPTH=4）

| 阶段 | 模块 | 输入通道 | 输出通道 | 空间尺寸 |
|------|------|----------|----------|----------|
| 输入 | – | 1 | – | 256×64 |
| Enc-1 | DoubleConv + MaxPool(2) | 1 | 16 | → 128×32 |
| Enc-2 | DoubleConv + MaxPool(2) | 16 | 32 | → 64×16 |
| Enc-3 | DoubleConv + MaxPool(2) | 32 | 64 | → 32×8 |
| Enc-4 | DoubleConv + MaxPool(2) | 64 | 128 | → 16×4 |
| Bottleneck | DoubleConv | 128 | 256 | 16×4 |
| Dec-4 | ConvTranspose + cat(skip) + DoubleConv | 256→128+128 | 128 | → 32×8 |
| Dec-3 | ConvTranspose + cat(skip) + DoubleConv | 128→64+64 | 64 | → 64×16 |
| Dec-2 | ConvTranspose + cat(skip) + DoubleConv | 64→32+32 | 32 | → 128×32 |
| Dec-1 | ConvTranspose + cat(skip) + DoubleConv | 32→16+16 | 16 | → 256×64 |
| Head | Conv2d(1×1) | 16 | 1 | 256×64 |

**DoubleConv 块：** Conv3×3 → BN → ReLU → Conv3×3 → BN → ReLU（均 `padding=1`，`bias=False`）

**跳跃连接：** `torch.cat([up(x), skip], dim=1)`，尺寸不匹配时用双线性插值对齐

**输出：** logits（未经 sigmoid）；推理时 `prob = sigmoid(logits)`

---

## 八、训练配置（Cell 18 / 23）

| 参数 | 变量名 | 值 | 说明 |
|------|--------|----|------|
| 批大小 | `BATCH_SIZE` | 8 | |
| 初始学习率 | `LR` | 3×10⁻⁴ | AdamW |
| 权重衰减 | `WEIGHT_DECAY` | 1×10⁻⁴ | L2 正则 |
| 训练轮数 | `N_EPOCHS` | 150 | |
| 正样本权重 | `POS_WEIGHT` | 50.0 | BCEWithLogitsLoss |
| 验证集比例 | `VAL_RATIO` | 0.15 | → ~14 帧 val，~82 帧 train |

**损失函数（加权 BCE）：**
$$
\mathcal{L} = -\frac{1}{N}\sum_{i}\left[w_+ \cdot y_i \log\hat{p}_i + (1-y_i)\log(1-\hat{p}_i)\right], \quad w_+ = 50
$$

**优化器：** AdamW（$\beta_1=0.9,\ \beta_2=0.999$）

**学习率调度：** 余弦退火

$$
lr(t) = \eta_{\min} + \frac{1}{2}(\eta_{\max} - \eta_{\min})\left(1 + \cos\frac{\pi t}{T_{\max}}\right)
$$

$\eta_{\max} = 3\times10^{-4}$，$\eta_{\min} = 10^{-6}$，$T_{\max} = 150$

---

## 九、数据增强（Cell 21）

| 增强方式 | 参数 | 实现 |
|----------|------|------|
| 水平翻转（方位轴） | `AUG_HFLIP_PROB = 0.5` | `img[:, :, ::-1]`（同步翻转标签）|
| 加性高斯噪声 | `AUG_NOISE_STD = 0.05` | $\mathcal{N}(0,\ 0.05^2)$，逐像素独立 |
| 亮度抖动 | `AUG_BRIGHT_JITTER = 0.15` | $\mathcal{U}(-0.15, 0.15)$，全图一致 |
| 范围截断 | – | `np.clip(img, 0, 1)` |

增强仅在训练集 `__getitem__` 中在线应用，验证集不增强。

---

## 十、推理与位置提取（Cell 25）

| 参数 | 变量名 | 值（当前） | 说明 |
|------|--------|-----------|----|
| 检测阈值 | `DET_THRESHOLD` | **0.1**（原 0.4） | sigmoid 后概率阈值 |
| 保存路径 | `SAVE_MODEL_PATH` | `'unet_sonar.pth'` | 最优权重 |

**位置提取算法：**

$$
\hat{r} = \frac{\sum_{(i,j): p_{ij}>\tau} p_{ij}\cdot r_i}{\sum p_{ij}}, \quad \hat{\theta} = \frac{\sum p_{ij}\cdot \theta_j}{\sum p_{ij}}
$$

当 $\max(p) \leq \tau$ 时，退化为 argmax 兜底：

$$
(\hat{r},\hat{\theta}) = (r_{i^*}, \theta_{j^*}), \quad (i^*,j^*) = \arg\max_{i,j} p_{ij}
$$

**置信度：** $\text{conf}_p = \max_{i,j} p_{ij}$（每帧记录，用于后续可视化颜色编码）

---

## 十一、关键假设汇总

| 假设 | 位置 | 影响 |
|------|------|------|
| 声速均匀，C = 1500 m/s | Cell 4 | 距离轴精度 |
| 远场平面波（均匀线阵） | Cell 6 | 波束成形精度 |
| 匀速运动（Kalman） | Cell 8 | 机动目标跟踪能力受限 |
| dt=1（PRI 未知） | Cell 8 | 速度单位为"米/帧"，非 m/s |
| 仅观测位置，不观测速度 | Cell 8 | 速度靠推算，初始收敛慢 |
| Kalman 输出即真值（伪标签） | Cell 20 | Kalman 失锁时标签污染 |
| 目标每帧出现在视场内 | Cell 8/25 | 全局重搜可能锁到杂波 |
| 各帧独立 min-max 归一化 | Cell 20 | 丢失帧间幅度信息 |
| 标签峰值归一为 1.0 | Cell 20 | BCEWithLogitsLoss 正样本目标恒为 1 |
