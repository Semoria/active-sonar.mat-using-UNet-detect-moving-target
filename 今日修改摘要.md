# 今日修改摘要 & 当前参数总览

---

## 一、今日修改内容（Cell 18 / 25 / 26）

### Cell 18 — UNet 超参数

| 参数 | 修改前 | 修改后 | 原因 |
|------|--------|--------|------|
| `DET_THRESHOLD` | `0.4` | **`0.1`** | 欠训练模型 sigmoid 输出通常远低于 0.4，阈值过高导致所有帧都不被更新（红线水平不动） |

---

### Cell 25 — UNet 推理

**问题根因：** 原代码当 `prob.max() < DET_THRESHOLD` 时，`pred_r[p]` / `pred_b[p]` 保持初始值（NaN 或常数），导致红线水平不动。

**修改内容：**

1. **添加 argmax 兜底** — 置信度不足时取 `prob.argmax()` 而非保持初始值
2. **新增 `conf` 数组** — 记录每帧 `prob.max()`，供可视化使用
3. **新增诊断输出** — 运行前打印第 0 帧 heatmap 动态范围

```python
# 修改前（有 silent fallback 问题）
mask = prob > threshold
if mask.any():
    ...更新 pred_r[p]
# else: 什么都不做 → pred_r[p] 永远是初始值

# 修改后
if prob.max() > threshold:
    mask = prob > threshold
    ry, bx = np.where(mask)
    weights = prob[ry, bx]
    pred_r[p] = float(np.average(r_ax[ry], weights=weights))
    pred_b[p] = float(np.average(b_ax[bx], weights=weights))
else:
    ri, bi = np.unravel_index(prob.argmax(), prob.shape)  # argmax 兜底
    pred_r[p] = r_ax[ri]
    pred_b[p] = b_ax[bi]
conf[p] = float(prob.max())

# 函数签名变化：现在返回 3 个值
unet_r, unet_b, unet_conf = unet_predict_all(...)
```

---

### Cell 26 — 可视化

**修改内容：**

- 用 `hi_conf = unet_conf > DET_THRESHOLD` / `lo_conf = ~hi_conf` 区分高/低置信帧
- 高置信帧实线绘制，argmax 兜底帧虚线半透明
- X-Y scatter 颜色改为按置信度 `unet_conf` 着色（原为脉冲编号）
- 末尾统计改为仅对高置信帧计算误差

---

## 二、当前代码整体参数框架

### ▶ Cell 8 — Kalman 滤波追踪（**尚未修改，计划下一步调整**）

```python
# === 可调参数 ===
MASK_RANGE    = 8.0    # 近场屏蔽阈值（m），小于此距离的回波被忽略
SEARCH_RADIUS = 2.0    # 预测位置附近搜索半径（m）← 过小，目标每帧移动约 3-6m
Q_STD         = 0.3    # 过程噪声（目标加速度不确定性, m）← 过小
R_STD         = 1.0    # 观测噪声（检测位置误差, m）

# Kalman 门限（在循环前定义）
INNOV_GATE    = 2.0    # 新息距离门限（m）← 过小，与 SEARCH_RADIUS 相同
MAX_COAST     = 1      # 最大连续 coasting 帧数，超过则全局重搜 ← 过小（1帧就重搜）

# 状态向量
state = [x, y, vx, vy]   # 笛卡尔坐标 + 速度（匀速模型）
dt    = 1.0               # 归一化时间步（实际 PRI 未知）

# 矩阵
F: 状态转移（匀速）
G: 过程噪声耦合矩阵
Q = Q_STD² × G @ G.T
H: 观测矩阵（只观测位置 x, y）
R = R_STD² × I₂

# 初始化
P₀ = 5.0 × I₄   # 初始不确定性

# 输出（转回极坐标）
bright_r = sqrt(kf_x² + kf_y²)
bright_b = degrees(arctan2(kf_x, kf_y))   # 方位角（deg）
```

**已知问题（待修复）：**
- `SEARCH_RADIUS=2.0` 太小 → 目标跑出搜索框 → `MAX_COAST=1` 触发全局重搜 → 锁到杂波
- 全局重搜后 `state = [mx, my, 0, 0]` 速度置零 → 破坏动量

---

### ▶ Cell 18 — UNet 超参数（**今日已修改**）

```python
# 数据维度
IMG_H       = 256      # resize 后图像高度（距离维）
IMG_W       = 64       # resize 后图像宽度（方位维）
N_CONTEXT   = 1        # 输入通道数（1 = 只用当前帧）

# 伪标签生成
LABEL_SIGMA = 3.0      # 高斯 blob 标准差（resize 后像素）

# 数据增强
AUG_HFLIP_PROB    = 0.5    # 水平翻转概率
AUG_NOISE_STD     = 0.05   # 加性高斯噪声标准差
AUG_BRIGHT_JITTER = 0.15   # 亮度抖动范围

# 模型结构
BASE_CH = 16    # 编码器第一层卷积核数
DEPTH   = 4     # UNet 下采样层数

# 训练
BATCH_SIZE   = 8        # 每批样本数
LR           = 3e-4     # 学习率（AdamW）
WEIGHT_DECAY = 1e-4     # L2 正则化
N_EPOCHS     = 150      # 训练轮数
POS_WEIGHT   = 50.0     # BCEWithLogitsLoss 正样本权重
VAL_RATIO    = 0.15     # 验证集比例

# 推理（今日修改）
DET_THRESHOLD   = 0.1   # ← 从 0.4 改为 0.1
SAVE_MODEL_PATH = 'unet_sonar.pth'
```

---

### ▶ Cell 20 — 标签生成（当前版本）

```python
# Gaussian blob 生成
make_label(r_m, b_deg, r_ax, b_ax, sigma):
    # 在 (r_m, b_deg) 处放一个峰值为 1 的高斯 blob
    # sigma = LABEL_SIGMA (3.0 px)

# 当前：对所有 N_PULSE 帧生成标签（包括 Kalman 失锁的坏帧）
labels = np.stack([make_label(bright_r[p], bright_b[p], ...) for p in range(N_PULSE)])
```

**注：** 修好 Kalman 后，此处的标签质量将自动提升。

---

### ▶ Cell 21-23 — Dataset / 训练

```python
# 数据集划分（随机打乱 96 帧）
val_size = int(N_PULSE * VAL_RATIO) = 14 帧
train_size = 82 帧

# 损失函数
criterion = BCEWithLogitsLoss(pos_weight=tensor([50.0]))

# 优化器
optimizer = AdamW(lr=3e-4, weight_decay=1e-4)
scheduler = CosineAnnealingLR(T_max=150, eta_min=1e-6)
```

---

## 三、下一步计划（Kalman 修复）

修改 Cell 8 参数：

| 参数 | 当前值 | 建议值 | 说明 |
|------|--------|--------|------|
| `SEARCH_RADIUS` | 2.0 m | **8.0 m** | 覆盖目标每帧移动范围 |
| `INNOV_GATE` | 2.0 m | **6.0 m** | 避免过度拒绝有效观测 |
| `MAX_COAST` | 1 | **4** | 允许短暂遮挡后恢复 |
| `Q_STD` | 0.3 | **1.0** | 增大过程噪声，预测更灵活 |
| `R_STD` | 1.0 | **1.5** | 与更大门限配合 |

同时修复全局重搜速度置零问题：
```python
# 原代码
state = np.array([mx, my, 0, 0])
# 修复后
state = np.array([mx, my, state[2], state[3]])
```

修复后 → 重跑 Cell 20（标签）→ 重训 UNet → 重跑 Cell 25-26（对比）。
